% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_image_url.R
\name{build_image_url}
\alias{build_image_url}
\title{Build full image URLs from camera metadata and filenames}
\usage{
build_image_url(camera_row, filename, size = c("small", "overlay", "thumb"))
}
\arguments{
\item{camera_row}{A single-row tibble (or named list) from \code{\link[=find_cameras]{find_cameras()}}.
Must contain the directory column corresponding to \code{size}.}

\item{filename}{Character vector of one or more filenames from
\code{\link[=list_images]{list_images()}}.}

\item{size}{Image size. One of:
\itemize{
\item \code{"small"} (default): ~720 px wide (\code{smallDir})
\item \code{"overlay"}: full-size overlay image (\code{overlayDir})
\item \code{"thumb"}: thumbnail ~200 px tall (\code{thumbDir})
}}
}
\value{
A character vector of full image URLs, one per element of
\code{filename}.
}
\description{
Combines a camera's base directory with one or more filenames returned by
\code{\link[=list_images]{list_images()}} to produce download-ready image URLs.
}
\examples{
\dontrun{
cam   <- find_cameras(cam_id = "WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire")
imgs  <- list_images(cam$camId[[1]], limit = 5)
urls  <- build_image_url(cam, imgs$filename, size = "small")
}
}
