% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_cameras.R
\name{find_cameras}
\alias{find_cameras}
\title{Query NIMS cameras}
\usage{
find_cameras(site_id = NULL, cam_id = NULL, return_fields = NULL)
}
\arguments{
\item{site_id}{Character. NWIS site number (e.g. \code{"05366800"}). Cannot be
used together with \code{cam_id}.}

\item{cam_id}{Character. Camera identifier (e.g.
\code{"WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire"}). Cannot be used together
with \code{site_id}.}

\item{return_fields}{Character vector of camera fields to include in the
response (e.g. \code{c("camName", "newestImageDT")}). \code{camId} is always
returned regardless of this setting. When \code{NULL} (the default), all fields
are returned.}
}
\value{
A tibble with one row per camera. Columns depend on \code{return_fields}.
\code{lat} and \code{lng} are returned as numeric. Datetime columns are POSIXct
(UTC). The \code{ingest} column is a list-column containing each camera's
ingestion configuration.
}
\description{
Returns a tibble of camera metadata from the USGS National Imagery
Management System (NIMS). With no arguments, all cameras are returned.
Filter by a NWIS site ID or a specific camera ID.
}
\examples{
\dontrun{
# All cameras
find_cameras()

# Cameras at a specific gage
find_cameras(site_id = "05366800")

# A specific camera
find_cameras(cam_id = "WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire")

# Only selected fields
find_cameras(return_fields = c("camName", "newestImageDT"))
}
}
