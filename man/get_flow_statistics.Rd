% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_flow_statistics.R
\name{get_flow_statistics}
\alias{get_flow_statistics}
\title{Retrieve historical flow statistics for a USGS gage}
\usage{
get_flow_statistics(
  site_id = NULL,
  cam_id = NULL,
  parameter_code = "00060",
  type = c("daily_normals", "period_summary"),
  computation = "percentile",
  time = NULL
)
}
\arguments{
\item{site_id}{Character. A single NWIS site number (e.g. \code{"05366800"} or
\code{"USGS-05366800"}). Exactly one of \code{site_id} or \code{cam_id} must be provided.}

\item{cam_id}{Character. A camera identifier. If supplied, the function
resolves the associated NWIS site ID via \code{\link[=find_cameras]{find_cameras()}}. Exactly one of
\code{site_id} or \code{cam_id} must be provided.}

\item{parameter_code}{Character. One or more five-digit USGS parameter codes.
Default \code{"00060"} (discharge, ft³/s).}

\item{type}{Character. \code{"daily_normals"} (default) returns day-of-year and
month-of-year percentile/statistic curves, suitable for overlaying on a
time series plot. \code{"period_summary"} returns calendar-month, calendar-year,
and water-year summaries.}

\item{computation}{Character vector. One or more of \code{"percentile"} (default),
\code{"arithmetic_mean"}, \code{"minimum"}, \code{"maximum"}, \code{"median"}. When \code{NULL} or
\code{NA}, all computation types are returned.}

\item{time}{For \code{type = "period_summary"} only. A POSIXct, Date, or character
vector of length 1 or 2 (start, end) used to restrict the date range of
the summary. Follows the same semantics as the \code{time} argument in
\code{\link[=get_site_streamflow]{get_site_streamflow()}}. \code{NULL} (default) returns the full period of
record. Ignored when \code{type = "daily_normals"}.}
}
\value{
A tibble. All types include columns:
\describe{
\item{\code{site_id}}{Bare NWIS site number.}
\item{\code{parameter_code}}{Five-digit parameter code.}
\item{\code{unit_of_measure}}{Units string.}
\item{\code{computation}}{Statistical method (e.g. \code{"percentile"}).}
\item{\code{percentile}}{Integer percentile (e.g. \code{50L}); \code{NA} for
non-percentile computations.}
\item{\code{value}}{Numeric statistic value.}
\item{\code{sample_count}}{Number of observations used to compute the
statistic.}
}
\code{type = "daily_normals"} additionally includes:
\describe{
\item{\code{month_day}}{Character MM-DD representing the day or month of year.}
}
\code{type = "period_summary"} additionally includes:
\describe{
\item{\code{interval_type}}{One of \code{"calendar_month"}, \code{"calendar_year"}, or
\code{"water_year"}.}
\item{\code{start_date}}{Date. Start of the summary interval.}
\item{\code{end_date}}{Date. End of the summary interval.}
}
Returns a zero-row tibble (with correct column types) when no data are
found. Requires the \link[dataRetrieval:dataRetrieval]{dataRetrieval} package.
}
\description{
Provides historical context for interpreting camera imagery — "is this
flow high or low?" — by returning day-of-year percentile curves or
period-of-record summaries.
}
\details{
Wraps \code{\link[dataRetrieval:read_waterdata_stats_por]{dataRetrieval::read_waterdata_stats_por()}} (\code{type = "daily_normals"})
or \code{\link[dataRetrieval:read_waterdata_stats_daterange]{dataRetrieval::read_waterdata_stats_daterange()}} (\code{type = "period_summary"}).
}
\examples{
\dontrun{
# Day-of-year discharge percentile curves (the 10/25/50/75/90th percentiles)
get_flow_statistics("05366800")

# Annual and monthly discharge summaries
get_flow_statistics("05366800", type = "period_summary")

# Restrict period_summary to a specific date range
get_flow_statistics(
  "05366800",
  type = "period_summary",
  time = c("2010-01-01", "2020-12-31")
)

# Multiple computation types
get_flow_statistics(
  "05366800",
  computation = c("minimum", "median", "maximum")
)

# Overlay on a streamflow time series
flow  <- get_site_streamflow("05366800", time = "P1Y")
stats <- get_flow_statistics("05366800")
}
}
