% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_site_field_measurements.R
\name{get_site_field_measurements}
\alias{get_site_field_measurements}
\title{Retrieve manual field measurements for a USGS gage}
\usage{
get_site_field_measurements(
  site_id = NULL,
  cam_id = NULL,
  time = NULL,
  parameter_code = "00060"
)
}
\arguments{
\item{site_id}{Character. A single NWIS site number (e.g. \code{"05366800"} or
\code{"USGS-05366800"}). Exactly one of \code{site_id} or \code{cam_id} must be provided.}

\item{cam_id}{Character. A camera identifier. If supplied, the function
resolves the associated NWIS site ID via \code{\link[=find_cameras]{find_cameras()}}. Exactly one of
\code{site_id} or \code{cam_id} must be provided.}

\item{time}{POSIXct, Date, or character vector of length 1 or 2 specifying
the time window. Follows the same semantics as \code{\link[=get_site_streamflow]{get_site_streamflow()}}.
\code{NULL} (default) returns all available measurements.}

\item{parameter_code}{Character. One or more five-digit USGS parameter codes.
Default \code{"00060"} (discharge, ft³/s). \code{NULL} returns measurements for all
parameters.}
}
\value{
A tibble with columns:
\describe{
\item{\code{site_id}}{Bare NWIS site number.}
\item{\code{datetime}}{POSIXct (UTC) of the measurement.}
\item{\code{value}}{Numeric measured value.}
\item{\code{unit}}{Units of measure (e.g. \code{"ft3/s"}).}
\item{\code{parameter_code}}{Five-digit parameter code.}
\item{\code{approval_status}}{\code{"Approved"} or \code{"Provisional"}.}
\item{\code{measurement_rated}}{Quality rating of the measurement:
\code{"Excellent"} (≤2\% error), \code{"Good"} (≤5\%), \code{"Fair"} (≤8\%),
or \code{"Poor"} (>8\%).}
\item{\code{control_condition}}{Description of channel control conditions
during the measurement.}
}
Returns a zero-row tibble (with correct column types) when no measurements
are found. Compatible with \code{\link[=get_site_streamflow]{get_site_streamflow()}} output via
\code{dplyr::bind_rows()} on the shared columns. Requires the \link[dataRetrieval:dataRetrieval]{dataRetrieval}
package.
}
\description{
Returns the record of in-person discharge measurements made by USGS
hydrographers at a gage site. These manual measurements are the "ground
truth" used to build stage-discharge rating curves, and they provide direct
visual correlates to camera imagery taken at the same location and time.
}
\details{
Wraps \code{\link[dataRetrieval:read_waterdata_field_measurements]{dataRetrieval::read_waterdata_field_measurements()}}.
}
\examples{
\dontrun{
# All discharge measurements at a site
get_site_field_measurements("05366800")

# Measurements during a specific period
get_site_field_measurements(
  "05366800",
  time = c("2020-01-01", "2024-12-31")
)

# Via camera ID
get_site_field_measurements(
  cam_id = "WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire"
)

# Compare field measurements against continuous sensor record
field <- get_site_field_measurements("05366800", time = "P2Y")
sensor <- get_site_streamflow("05366800", time = "P2Y")
}
}
