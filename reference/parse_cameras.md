# Parse a list of camera objects into a tibble

Parse a list of camera objects into a tibble

## Usage

``` r
parse_cameras(result)
```

## Arguments

- result:

  List returned by `nims_request("/cameras")`.

## Value

A tibble.
