% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_site_data_availability.R
\name{get_site_data_availability}
\alias{get_site_data_availability}
\title{Discover available time series at a USGS gage}
\usage{
get_site_data_availability(
  site_id = NULL,
  cam_id = NULL,
  parameter_code = NA_character_
)
}
\arguments{
\item{site_id}{Character. A single NWIS site number (e.g. \code{"05366800"} or
\code{"USGS-05366800"}). Exactly one of \code{site_id} or \code{cam_id} must be provided.}

\item{cam_id}{Character. A camera identifier. If supplied, the function
resolves the associated NWIS site ID via \code{\link[=find_cameras]{find_cameras()}}. Exactly one of
\code{site_id} or \code{cam_id} must be provided.}

\item{parameter_code}{Character. One or more five-digit USGS parameter codes
to filter results (e.g. \code{"00060"} for discharge). Default \code{NA} returns all
available parameters.}
}
\value{
A tibble with columns:
\describe{
\item{\code{site_id}}{Bare NWIS site number (without \verb{USGS-} prefix).}
\item{\code{parameter_code}}{Five-digit parameter code.}
\item{\code{parameter_name}}{Human-readable parameter name.}
\item{\code{unit_of_measure}}{Units string (e.g. \code{"ft3/s"}).}
\item{\code{begin_utc}}{POSIXct (UTC). Earliest observation in this time series.}
\item{\code{end_utc}}{POSIXct (UTC). Most recent observation.}
\item{\code{statistic_id}}{Five-digit statistic code (e.g. \code{"00003"} for mean).}
\item{\code{time_series_id}}{Unique identifier for this time series.}
}
Rows are sorted by \code{parameter_code}. Returns a zero-row tibble when no
time series are found. Requires the \link[dataRetrieval:dataRetrieval]{dataRetrieval} package.
}
\description{
Returns metadata about every time series recorded at a site — parameter
codes, units, and period of record — so you know exactly which codes to
pass to \code{\link[=get_site_streamflow]{get_site_streamflow()}}.
}
\details{
Wraps \code{\link[dataRetrieval:read_waterdata_ts_meta]{dataRetrieval::read_waterdata_ts_meta()}}.
}
\examples{
\dontrun{
# All parameters at a site
get_site_data_availability("05366800")

# Discharge only
get_site_data_availability("05366800", parameter_code = "00060")

# Via camera ID
get_site_data_availability(
  cam_id = "WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire"
)

# Chain with get_site_streamflow():
avail <- get_site_data_availability("05366800")
flow  <- get_site_streamflow("05366800",
                             parameter_code = avail$parameter_code[1])
}
}
