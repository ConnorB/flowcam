% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_site_streamflow.R
\name{get_site_streamflow}
\alias{get_site_streamflow}
\title{Retrieve streamflow data for a USGS gage}
\usage{
get_site_streamflow(
  site_id = NULL,
  cam_id = NULL,
  time = NULL,
  parameter_code = "00060",
  type = c("continuous", "daily")
)
}
\arguments{
\item{site_id}{Character. A single NWIS site number (e.g. \code{"05366800"} or
\code{"USGS-05366800"}). Exactly one of \code{site_id} or \code{cam_id} must be provided.}

\item{cam_id}{Character. A camera identifier (e.g.
\code{"WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire"}). If supplied, the
function resolves the associated NWIS site ID via \code{\link[=find_cameras]{find_cameras()}}.
Exactly one of \code{site_id} or \code{cam_id} must be provided.}

\item{time}{POSIXct, Date, or character vector of length 1 or 2. Follows
the same semantics as the \code{time} argument in \code{\link[=list_images]{list_images()}}: length-1 is
treated as a start bound; length-2 sets start and end, with \code{NA} for an
open bound. ISO 8601 duration strings such as \code{"P7D"} (last 7 days) or
\code{"P1Y"} (last year) are also accepted and forwarded to the API unchanged.
When \code{NULL}, the API default applies (approximately the last year of data
for continuous; all available data for daily).}

\item{parameter_code}{Character. Five-digit USGS parameter code. Default
\code{"00060"} is discharge in cubic feet per second. Other common codes:
\code{"00010"} (water temperature, \eqn{°C}), \code{"00065"} (gage height, ft),
\code{"00095"} (specific conductance, µS/cm).}

\item{type}{Character. \code{"continuous"} (default) returns instantaneous
(unit-value) observations; \code{"daily"} returns daily mean values.}
}
\value{
A tibble with columns:
\describe{
\item{\code{site_id}}{Bare NWIS site number (without \verb{USGS-} prefix).}
\item{\code{datetime}}{POSIXct (UTC) for \code{type = "continuous"}; Date for
\code{type = "daily"}.}
\item{\code{value}}{Numeric observation value.}
\item{\code{unit}}{Units of measure (e.g. \code{"ft3/s"}).}
\item{\code{parameter_code}}{Five-digit parameter code.}
\item{\code{approval_status}}{\code{"Approved"} or \code{"Provisional"}.}
}
Returns a zero-row tibble (with correct column types) when no data are
found. Requires the \link[dataRetrieval:dataRetrieval]{dataRetrieval} package.
}
\description{
Wraps \code{\link[dataRetrieval:read_waterdata_continuous]{dataRetrieval::read_waterdata_continuous()}} (instantaneous values)
or \code{\link[dataRetrieval:read_waterdata_daily]{dataRetrieval::read_waterdata_daily()}} (daily mean values) for the
USGS gage associated with a flowcam camera. Returns a clean tibble aligned
with flowcam conventions.
}
\examples{
\dontrun{
# Instantaneous discharge for the last 7 days
get_site_streamflow("05366800", time = "P7D")

# Daily discharge for a calendar year
get_site_streamflow(
  "05366800",
  time = c("2024-01-01", "2024-12-31"),
  type = "daily"
)

# Water temperature from a camera ID
get_site_streamflow(
  cam_id = "WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire",
  parameter_code = "00010"
)

# Pair with image timestamps
images <- list_images("05366800", time = c("2024-10-01", "2024-10-07"),
                      raw_item = TRUE)
flow   <- get_site_streamflow("05366800",
                              time = c("2024-10-01", "2024-10-07"))
}
}
