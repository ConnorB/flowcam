% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_network_cameras.R
\name{get_network_cameras}
\alias{get_network_cameras}
\title{Find cameras on the same stream network}
\usage{
get_network_cameras(
  site_id = NULL,
  cam_id = NULL,
  direction = c("upstream", "downstream", "both"),
  distance_km = 100
)
}
\arguments{
\item{site_id}{Character. A single NWIS site number (e.g. \code{"05366800"} or
\code{"USGS-05366800"}). Exactly one of \code{site_id} or \code{cam_id} must be provided.}

\item{cam_id}{Character. A camera identifier. If supplied, the function
resolves the associated NWIS site ID via \code{\link[=find_cameras]{find_cameras()}}. Exactly one of
\code{site_id} or \code{cam_id} must be provided.}

\item{direction}{Character. Navigation direction(s): \code{"upstream"} (upstream
tributary), \code{"downstream"} (downstream mainstem), or \code{"both"}. Default
\code{"upstream"}.}

\item{distance_km}{Numeric. How far (in kilometres) to navigate along the
stream network from the origin site. Default \code{100}.}
}
\value{
A tibble with one row per camera (including the origin site) and
columns:
\describe{
\item{\code{site_id}}{Bare NWIS site number.}
\item{\code{cam_id}}{Camera identifier.}
\item{\code{cam_name}}{Human-readable camera name.}
\item{\code{direction}}{\code{"origin"}, \code{"upstream"}, or \code{"downstream"}.}
\item{\code{lat}}{Latitude (decimal degrees, WGS84).}
\item{\code{lng}}{Longitude (decimal degrees, WGS84).}
}
Returns a zero-row tibble when no networked cameras are found (including
the origin site if it has no camera). Requires the \link[dataRetrieval:dataRetrieval]{dataRetrieval} package.
}
\description{
Uses the USGS Network Linked Data Index (NLDI) to navigate upstream and/or
downstream from a gage site, then returns all flowcam cameras found along
the network within a specified distance.
}
\details{
Useful for anticipating flood wave arrival, comparing conditions across a
watershed, or discovering monitoring locations on the same river system.

Wraps \code{\link[dataRetrieval:findNLDI]{dataRetrieval::findNLDI()}}.
}
\examples{
\dontrun{
# Cameras upstream of a site (within 100 km)
get_network_cameras("05366800")

# Cameras both upstream and downstream (150 km)
get_network_cameras("05366800", direction = "both", distance_km = 150)

# Via camera ID
get_network_cameras(
  cam_id = "WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire",
  direction = "downstream"
)
}
}
