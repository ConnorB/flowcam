% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_video.R
\name{make_video}
\alias{make_video}
\title{Assemble camera images into an MP4 video}
\usage{
make_video(
  cam_id = NULL,
  site_id = NULL,
  time = NULL,
  output = NULL,
  fps = 2,
  size = "small",
  limit = 1000L,
  dir = NULL,
  one_per_day = FALSE
)
}
\arguments{
\item{cam_id}{Character. Camera identifier. Cannot be used with \code{site_id}.}

\item{site_id}{Character. NWIS site number (e.g. \code{"05366800"} or
\code{"USGS-05366800"}). Cannot be used with \code{cam_id}.}

\item{time}{POSIXct, Date, or character vector of length 1 or 2. Same
semantics as \code{\link[=download_images]{download_images()}}. When \code{dir} is supplied, timestamps are
parsed from the filenames (NIMS format: \verb{<camId>___<timestamp>.jpg}).}

\item{output}{Character. File path for the output MP4. Defaults to
\code{"<cam_id>.mp4"} (or \code{"<site_id>.mp4"}, or the directory basename) in the
working directory.}

\item{fps}{Positive number. Frames per second. Any positive value is
accepted. Default is \code{2}.}

\item{size}{Image size passed to \code{\link[=download_images]{download_images()}}. One of \code{"small"}
(default), \code{"overlay"}, or \code{"thumb"}. Ignored when \code{dir} is supplied.}

\item{limit}{Integer. Page size for the internal \code{\link[=list_images]{list_images()}} call.
Default is \code{1000}. Ignored when \code{dir} is supplied.}

\item{dir}{Character. Path to a local directory of already-downloaded images.
When supplied, downloads are skipped and JPEG/PNG files are read from this
directory. \code{cam_id}/\code{site_id} and \code{time} still apply as filters.}

\item{one_per_day}{Logical. If \code{TRUE}, reduce frames to one per calendar day
by selecting the image whose capture time is closest to noon in the
camera's local timezone (from the \code{tz} field of \code{\link[=find_cameras]{find_cameras()}}). Default
is \code{FALSE}.}
}
\value{
The output file path, invisibly.
}
\description{
Downloads images for a camera over a specified time range and encodes them
into an MP4 video using the \code{av} package. Provide either \code{cam_id} or
\code{site_id} to identify the camera, and use \code{time} to restrict the range.
}
\details{
When \code{dir} is supplied, images are read from that local directory instead of
being downloaded. You can still pass \code{cam_id}/\code{site_id} to select only files
belonging to a particular camera (matched by filename prefix) and \code{time} to
filter by timestamp embedded in the filename — useful when a directory
contains images from multiple cameras or a wider date range than needed.
}
\examples{
\dontrun{
# Download and assemble images for a date range
make_video("WI_Chippewa_River_at_Grand_Ave_at_Eau_Claire",
           time = c("2025-06-01", "2025-06-02"), output = "chippewa.mp4")

# One frame per day from a local directory
make_video(cam_id = "NM_Pecos_Web_Camera_near_Roswell",
           time        = c("2023-08-01", "2023-08-31"),
           dir         = "~/Downloads/Pecos",
           one_per_day = TRUE,
           output      = "pecos_august.mp4")
}
}
